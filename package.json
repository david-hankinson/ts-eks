{
  "devDependencies": {
    "@types/node": "^14"
  },
  "dependencies": {
    "@pulumi/aws": "^6.67.0",
    "@pulumi/pulumi": "^3.147.0",
    "ts-node": "^10.9.1"
  },
  "scripts": {
    "start": "./node_modules/ts-node/dist/bin.js index.ts",
    "pulumi-destroy": "pulumi destroy --stack non-prod && pulumi destroy --stack prod"
  }
}
