name: ts-eks
description: Modular typescript pulumi code to provision an EKS cluster
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
